# --- web api ---
fastapi
uvicorn[standard]
pydantic>=2.6,<3
python-dotenv
requests
orjson

# --- core numerics / rerank ---
numpy
scikit-learn

# --- PDF / text ---
pdfplumber           # brings pdfminer.six
pypdf                # lightweight utilities (optional but handy)
regex

# --- optional LLM client(s) ---
openai>=1.30
tiktoken

# --- optional data stores (comment out if not needed) ---
# redis
# psycopg2-binary
# neo4j

# --- optional ML / vector (comment out if you don’t need them) ---
# sentence-transformers
# faiss-cpu

# --- optional OCR (if you actually use it) ---
# pillow
# pytesseract
